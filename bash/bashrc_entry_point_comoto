#!/bin/bash

# TODO rename this file ../bashrc/entrypoint/comoto
# TODO maybe while I'm creating all these symlinks... maybe I should just have a symlink at ~/.jjc_config_dir that everything points thru...

# pnpm
# TODO this jc isn't necessarilly correct...
export PNPM_HOME="/home/jc/.local/share/pnpm"
export PATH="$PNPM_HOME:$PATH"
# pnpm end

# TODO should this be declared here?
export JJC_CONFIG_ROOT="$HOME/projects/config"
. $JJC_CONFIG_ROOT/bash/bashrc

# TODO why is this here?
# Shouldn't it be in the nvim dir?
export MYVIMRC=~/projects/config/neovim/config/init.lua
source /usr/share/google-cloud-sdk/completion.bash.inc
source /home/jc/projects/comoto/monorepo/zlaverse/dev/../../zlaverse/support/bash_functions.sh
