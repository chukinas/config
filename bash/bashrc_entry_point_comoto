# pnpm
# TODO this jc isn't necessarilly correct...
export PNPM_HOME="/home/jc/.local/share/pnpm"
export PATH="$PNPM_HOME:$PATH"
# pnpm end

dotfiles_dir=~/projects/config
source "${dotfiles_dir}/bash/bashrc"

# TODO why is this here?
# Shouldn't it be in the nvim dir?
export MYVIMRC=~/projects/config/neovim/config/init.lua
